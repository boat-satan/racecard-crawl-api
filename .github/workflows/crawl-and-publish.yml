name: Crawl & Publish

on:
  # 手動実行
  workflow_dispatch:
    inputs:
      racers:
        description: "RACERS (例: 4349,3156) 空ならtodayの全出走者"
        required: false
        default: ""
      batch:
        description: "一度に処理する人数 (空=制限なし)"
        required: false
        default: ""
      delayMs:
        description: "1選手ごとの待機(ms)"
        required: false
        default: "3000"
  # 毎朝 05:00 JST（= 前日 20:00 UTC）
  schedule:
    - cron: "0 20 * * *"

permissions:
  contents: write

concurrency:
  group: crawl-publish
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps
        run: npm install --no-audit --no-fund

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Run crawler
        run: node scripts/crawl.js

      - name: Fetch racer stats
        env:
          RACERS: ${{ inputs.racers }}
          STATS_BATCH: ${{ inputs.batch }}
          STATS_DELAY_MS: ${{ inputs.delayMs || '3000' }}
        run: node scripts/fetch-stats.js

      - name: Commit & Push changes
        run: |
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "Update racer stats [skip ci]"
            git push
          else
            echo "No changes to commit."
          fi
